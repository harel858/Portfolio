{
  "version": 2,
  "builds": [
    {
      "src": "src/*.ts",
      "use": "@vercel/node-12",
      "config": {
        "outDir": "api"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
